# Core transit-photometry stack (pinned to avoid the NumPy-2 ABI conflict with
# transitleastsquares / batman, which are compiled against NumPy < 2).
numpy<2
astropy<7
lightkurve
transitleastsquares
wotan
batman-package
emcee
corner
scipy

# Classifier (tabular)
lightgbm
scikit-learn
imbalanced-learn
pandas
pyarrow
joblib

# Classifier (CNN, Track B — dual-view 1D-CNN). CPU build is plenty for these small
# 1D convnets; on Colab/GPU plain `torch` works too:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch

# Data access
astroquery

# App
streamlit
matplotlib
